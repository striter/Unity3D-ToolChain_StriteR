using Unity.Mathematics;

namespace Runtime.Geometry.Extension
{
    public partial class UGeometry
    {
        //https://paulbourke.net/geometry/pointlineplane/
        // public static float Distance(this G2Line _line, float2 _point)
        // {
        //     var p1 = _line.start;
        //     var p2 = _line.end;
        //     var p3 = _point;
        //     var u = ((p3.x -p1.x)*(p2.x - p1.x) + (p3.y - p1.y)*(p2.y - p1.y)) / _line.sqrLength;
        //     var p = p1 + u * (p2 - p1);
        //     return (p - _point).magnitude();
        // }
        //
    }
}